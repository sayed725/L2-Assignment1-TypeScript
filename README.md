
# 🎯 Interview Questions - Blog Task


## 1. What are some differences between interfaces and types in TypeScript?

### Answer: 
 
TypeScript-এ type এবং interface — দুটোই object-এর গঠন বা কাঠামো নির্ধারণ করতে ব্যবহৃত হয়।
এগুলো অনেক দিক থেকেই একে অপরের মতো, তবে কিছু সূক্ষ্ম পার্থক্য আছে।
নিচে Type এবং Interface-এর প্রধান পার্থক্যগুলো তুলে ধরা হলো: - 

1. Syntax (লিখনরীতি): Type নির্ধারণ করার জন্য type কীওয়ার্ড ব্যবহার করা হয়, আর interface নির্ধারণ করার জন্য interface কীওয়ার্ড ব্যবহার করা হয়।
উদাহরণস্বরূপ:


```bash

 type MyType = {
  prop1: string;
  prop2: number;
};

interface MyInterface {
  prop1: string;
  prop2: number;
}

```

2. Declaration Merging (ঘোষণা একত্রকরণ): TypeScript-এ interface-এর একটি বিশেষ বৈশিষ্ট্য আছে, যাকে বলা হয় declaration merging।
এর মাধ্যমে আপনি একটি interface-কে আবার ঘোষণা করে তার ভেতর নতুন property যোগ করতে পারেন।
অর্থাৎ, একটি interface-এর সংজ্ঞা আপনি একাধিক অংশে ভাগ করে লিখতে পারবেন।

অন্যদিকে, type-কে merge করা যায় না। একই নামে একাধিক type ঘোষণা করলে error দেখাবে।

নিচে interface declaration merging-এর একটি উদাহরণ দেওয়া হলো:


```bash

interface MyInterface {
  prop3: boolean;
}

interface MyInterface {
  prop4: string;
}

```

3. Extending (প্রসারিত করা): Types এবং interfaces — দুটোই extend করা যায়, তবে এদের syntax একটু আলাদা। Interface extend করতে extends কীওয়ার্ড ব্যবহার করা হয়,
আর type extend করতে ব্যবহৃত হয় intersection operator (&)।

নিচে একটি উদাহরণ দেওয়া হলো:

```bash

interface BaseInterface {
  prop1: string;
}

interface ExtendedInterface extends BaseInterface {
  prop2: number;
}

type BaseType = {
  prop1: string;
};

type ExtendedType = BaseType & {
  prop2: number;
};

```

4. Implementation (বাস্তবায়ন): Interface কোনো class দ্বারা বাস্তবায়ন করা যায়। অর্থাৎ, একটি class স্পষ্টভাবে ঘোষণা করতে পারে যে সে একটি নির্দিষ্ট interface বাস্তবায়ন করছে, এবং সেই interface-এ যে সব সদস্য (properties/methods) আছে সেগুলোর বাস্তবায়ন class-টিকে করতেই হবে।
অন্যদিকে, type সরাসরি কোনো class দ্বারা বাস্তবায়ন করা যায় না।




5. Use Cases (ব্যবহারক্ষেত্র): ধরাবাঁধা নিয়ম অনুযায়ী, TypeScript-এ interface সাধারণত object-এর গঠন (shape) ও চুক্তি (contract) নির্ধারণে ব্যবহৃত হয়। অন্যদিকে type ব্যবহার করা হয় union, intersection এবং আরও জটিল টাইপের কাজের জন্য। তবে TypeScript-এর সাম্প্রতিক সংস্করণগুলোতে interface এবং type-এর মধ্যকার এই সীমারেখা অনেকটাই অস্পষ্ট হয়ে গেছে, এবং এখন অনেক ক্ষেত্রেই এগুলো একে অপরের পরিবর্তে ব্যবহার করা যায়।




সর্বোপরি, interface বা type ব্যবহার করবেন কিনা—তা নির্ভর করে আপনার কোডবেসের প্রয়োজন এবং ব্যক্তিগত পছন্দের ওপর। TypeScript-এ দুটোই খুব শক্তিশালী টুল, এবং একই ধরনের কাজ দুটো দিয়েই করা সম্ভব।




## 2. What is the use of the keyof keyword in TypeScript? Provide an example.

### Answer:

keyof হলো TypeScript-এর একটি utility যা কোনো object type-এর সব key-এর union type তৈরি করে।
এটি মূলত safer এবং type-safe কোড লেখার জন্য ব্যবহৃত হয়, যাতে ভুল key ব্যবহার না হয়।


```bash

interface Person {
  name: string;
  age: number;
}

// keyof ব্যবহার করে সব key-এর union type তৈরি করা
type PersonKeys = keyof Person; 
// PersonKeys = "name" | "age"

// ব্যবহার
let key: PersonKeys;

key = "name"; // ঠিক আছে
key = "age";  // ঠিক আছে
// key = "email"; ❌ error, কারণ Person-এর মধ্যে "email" নেই

```

সর্বোপরি, keyof দিয়ে আমরা object type-এর সব key union type হিসেবে পেতে পারি।
এটি type-safe কোড তৈরি করতে সাহায্য করে এবং ভুল key access থেকে রক্ষা করে।




## 3. Explain the difference between any, unknown, and never types in TypeScript.

### Answer:

TypeScript-এ any, unknown, এবং never হলো non-primitive টাইপ। এরা  প্রিমিটিভ টাইপের মতো সরাসরি value ধারণ করে না 

1️⃣ any

- any টাইপ হলো সবকিছু হতে পারে এমন টাইপ।

- এটি type-checking এ কোনো বাধা দেয় না।

- তবে খুব বেশি ব্যবহার করলে TypeScript-এর type safety হারায়। 

```bash

let value: any;

value = 5;      // Number type ঠিক আছে
value = "Hello"; // String type ঠিক আছে
value = true;    // boolean ঠিক আছে

```

2️⃣ unknown

- unknown টাইপও যেকোনো value রাখতে পারে, কিন্তু অ্যাক্সেস করার আগে type চেক করতে হবে।

- এটি any এর চেয়ে safer।


```bash

let value: unknown;

value = 5;       
value = "Hello";

// let num: number = value; ❌ error
if (typeof value === "number") {
  let num: number = value; // type চেক করার পরে  ঠিক আছে
}

```

3️⃣ never

- never টাইপ এমন value বোঝায় যা কখনো উপস্থিত হবে না।

- সাধারণত ব্যবহার হয় error throw করা বা অপ্রাপ্ত function return এর জন্য।


```bash

function throwError(message: string): never {
  throw new Error(message);
}

function infiniteLoop(): never {
  while(true) {}
}

```



## 4. Explain the difference between any, unknown, and never types in TypeScript.

### Answer:

TypeScript-এ Enum হলো একটি feature যা নাম দিয়ে constant-এর group তৈরি করতে সাহায্য করে।
Enums ব্যবহার করলে কোড readable, maintainable এবং type-safe হয়।


1️⃣ String Enum এর উদাহরণ  (Role) : 

```bash

//আমরা Role নামের Enum তৈরি করেছি।

enum Role {
  Admin = "ADMIN",
  User = "USER",
  Moderator = "MOD"
}

//প্রতিটি নামের সাথে string value assign করা হয়েছে।

let myRole: Role;

//এখন myRole কেবল Enum-এর value নিতে পারবে।

myRole = Role.Admin;
console.log(myRole); // Enum-এর value "ADMIN"

myRole = Role.User;
console.log(myRole); // Enum-এর value "USER"

myRole = Role.Moderator;
console.log(myRole); // Enum-এর value "MOD"

```

2️⃣ Numeric Enum এর উদাহরণ  (Role) : 

```bash

//এখানে Enum value সংখ্যা হিসেবে assign করা হয়েছে।

enum Role {
  Admin = 1,
  User,
  Moderator
}

// প্রথম value 1 দেওয়া হয়েছে, এরপর স্বয়ংক্রিয়ভাবে 2, 3 হয়ে যায়।

let myRole: Role = Role.User;
console.log(myRole); // স্বয়ংক্রিয়ভাবে 2 হবে 

```


## 5. Provide an example of using union and intersection types in TypeScript.

### Answer:

1️⃣ Union Type (|)

Union type দিয়ে একটি ভেরিয়েবল একাধিক টাইপ নিতে পারে।

```bash

let value: string | number;
// এখানে value শুধুমাত্র string বা number হতে পারবে 

value = "Hello"; // ঠিক আছে
value = 42;      // ঠিক আছে
// value = true; ❌ error, কারণ boolean এখানে সমর্থিত নয়

```

2️⃣ Intersection Type (&)

Intersection type দিয়ে একটি object একাধিক type-এর সব property রাখতে হবে।


```bash

type Person = {
  name: string;
  age: number;
};

type Employee = {
  employeeId: number;
};

type EmployeePerson = Person & Employee; //Person & Employee মানে সব property থাকা লাগবে।

const emp: EmployeePerson = {
  name: "Rakib",
  age: 25,
  employeeId: 101
};

//এখন emp -এ  name, age এবং employeeId সব থাকতে হবে।

console.log(emp);

```

